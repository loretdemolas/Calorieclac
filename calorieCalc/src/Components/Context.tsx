import { createContext } from "react";

export const AgeContext = createContext(1);
export const WeightContext = createContext(1);
export const HeightContext = createContext(1);
export const ActivityLevelContext = createContext(1);
export const SexContext = createContext('male');

